# Swipey AI Guide visual QA

Status: passed local production preview on 2026-09-21.

Target checks:

- Desktop: 1440 × 1000 passed in Chrome; hero copy remains readable over the licensed image, Feed preview stays clear of navigation and photo credit.
- Mobile: 390 × 844 passed in Firefox after tightening the hero type scale and container sizing; the full header, menu, image crop, copy, and CTA pair fit without horizontal clipping.
- Accessibility: exactly one H1 per page, useful image alt text, visible keyboard focus, reduced-motion fallback, sufficient text contrast.
- SEO/GEO: crawlable answer-first copy, primary sources, Article/FAQ/WebSite JSON-LD, canonical URLs, sitemap, RSS, robots, Open Graph, and transparent editorial identity passed the static audit across 11 HTML pages.
